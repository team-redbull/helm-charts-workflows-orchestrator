apiVersion: v2
name: workflows-orchestrator
description: Temporal workflow "brain" worker — polls the workflow task queue for every workflow domain registered under workflows/. One deployment, shared across all domains (see the per-domain limb charts, e.g. helm-charts-segment-connectivity).
type: application
version: 0.1.0
appVersion: "0.1.0"
